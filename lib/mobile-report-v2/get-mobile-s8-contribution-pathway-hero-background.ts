import { getMobileIntegratedBlueprintBackgroundUrl } from "@/lib/mobile-report-v2/get-mobile-integrated-blueprint-background";

/** Mountain / light-path hero art behind S8 Contribution Pathway hero + essence. */
export function getMobileS8ContributionPathwayHeroBackgroundUrl(): string {
  return getMobileIntegratedBlueprintBackgroundUrl();
}
