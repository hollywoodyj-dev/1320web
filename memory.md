# 1320 Website Memory

## Project facts

- Workspace path: `C:\github\1320-website\web`
- Stack: Next.js (App Router) + TypeScript + Tailwind + ESLint
- Bootstrap verification: `npm run lint` and `npm run build` passed on scaffold

## Decisions

- Keep implementation mobile-first.
- Keep sections/content aligned to the provided UI/spec.
- Prefer simple structure and small iterative milestones.

## Open threads

- Phase 1 handoff: **`PHASE1_HANDOFF.md`** — **15/15 complete**. Anchor IA: `/blueprint#s1–s0`; report modules + sidebar on `/result` & `/sample-report`; no per-S public pages until Phase 2 (`/blueprint/s1-origin-frequency` etc.).
- UI 2/2: `homepage-ui.png` · `generating-ui.png` (`/generating`) · **`report-ui.jpeg`** (report dashboard → `/sample-report`, sidebar pattern for `/result`).
- Batch 4 ✅ + Lumen QA Pass (immersive `/generating`). Next: **Batch 6** (report UI + `/sample-report`) then Batch 5 (`/result`).
- Homepage: text updates allowed; layout/images frozen.

## Handoff notes

- If a change affects IA, tone, or conversion flow, record before/after rationale.
- After each milestone, append what was shipped and what remains.
